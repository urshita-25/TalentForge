# Flask Config
PORT=5000
FLASK_ENV=development

# Gemini API Key
# Get one from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# MongoDB Config (Optional: falls back to JSON mock file in data/ if not provided or connection fails)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/talentforge?retryWrites=true&w=majority
